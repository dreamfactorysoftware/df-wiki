# Staging environment variables for docker-compose.staging-wiki.yml
# Copy to .env.staging and fill in values before starting the stack.
#
#   cp .env.staging.example .env.staging

# --- MySQL ---
MYSQL_ROOT_PASSWORD=CHANGE_ME
MW_DB_NAME=staging_wiki
MW_DB_USER=CHANGE_ME
MW_DB_PASSWORD=CHANGE_ME

# --- MediaWiki secrets ---
# Generate with: openssl rand -hex 32
MW_SECRET_KEY=CHANGE_ME
# Generate with: openssl rand -hex 8
MW_UPGRADE_KEY=CHANGE_ME
